# Local QA Report — 2026-04-01

## QA Pass #1

**Environment:** http://localhost:3000 (API: http://localhost:5221)
**Branch:** bug/local-qa-2026-04-01
**Overall:** FAIL — 2 blocking issues found

### Blocking Issues

1. **Analytics endpoints return 500** — All 4 analytics endpoints crash with `UnResolvableVariableException` on nullable GUID parameter. Wolverine HTTP code generation cannot resolve `System.Nullable<System.Guid>`.
2. **Create Course fails for admin-created organizations** — `POST /courses` looks up org in legacy `Tenants` table but admin-created orgs only exist in `Organizations` table. Returns 400 "Tenant does not exist."

### Minor Issues (not fixing this cycle)

1. **Duplicate permissions in `/auth/me` response** — Permissions array contains duplicates (e.g., `["Admin", "Admin"]`).
2. **Operator tee sheet shows course name twice** — Header renders course name in two adjacent elements.
3. **Golfer pages blank with no guidance** — Tee times, bookings, and profile pages render shells but show no data and no empty-state message.
4. **SMS Log page empty with no empty-state message** — Page loads but shows blank content area.
5. **Add Golfer dialog submits silently with incomplete data** — No visible validation errors when required fields are blank.

### Happy Paths Verified

- Admin: View dashboard — PASS
- Admin: Create organization — PASS
- Admin: Create user (invite) — PASS
- Admin: View feature flags — PASS
- Admin: Create course (seeded tenant) — PASS
- Operator: View tee sheet — PASS
- Operator: Open waitlist — PASS
- Operator: Post tee time — PASS
- Public: Walkup join page — PASS

## QA Pass #2

**Note:** The running app serves code from the main repo, not the worktree. Fixes committed to `bug/local-qa-2026-04-01` could not be verified at runtime. The QA tester confirmed the same root causes are still present in the running code, validating the diagnosis. Code review confirmed both fixes are correct.

### Fixes Applied (in worktree)

1. **Analytics endpoints** — Removed `[NotBody]` from `Guid?` and `int` query params on all 4 GET endpoints. Commit: `4763678`
2. **Course creation** — Changed validation to check Organizations first, fall back to Tenants with bridge row. Updated all course query endpoints to join on Organizations. Commit: `a66e859`

### Status

Both fixes committed, reviewed (LGTM), and ready for PR.
