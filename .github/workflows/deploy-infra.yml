# Deploy Bicep Infrastructure
#
# Subscription-level deployment (Bicep creates resource groups):
#   1. Shared infrastructure (ACR) → teeforce-shared-rg
#   2. Environment infrastructure  → teeforce-{env}-rg
#
# Triggered manually or called by other workflows.

name: Deploy Infrastructure

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment (test, staging, prod)'
        required: true
        type: choice
        options:
          - test
          - staging
          - prod
  workflow_call:
    inputs:
      environment:
        description: 'Target environment (test, staging, prod)'
        required: true
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Deploy Bicep (${{ inputs.environment }})
    runs-on: ubuntu-latest
    env:
      LOCATION: eastus2
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy shared infrastructure
        run: |
          az deployment sub create \
            --name "teeforce-shared" \
            --location ${{ env.LOCATION }} \
            --parameters infra/bicep/parameters.shared.bicepparam

      - name: Get current container image tag
        id: current-image
        run: |
          TAG=$(az containerapp show \
            --name "teeforce-app-${{ inputs.environment }}" \
            --resource-group "teeforce-${{ inputs.environment }}-rg" \
            --query "properties.template.containers[0].image" \
            --output tsv 2>/dev/null \
            | sed 's/.*://')
          echo "tag=${TAG:-latest}" >> "$GITHUB_OUTPUT"
          echo "Using image tag: ${TAG:-latest}"

      - name: Deploy environment infrastructure
        run: |
          az deployment sub create \
            --name "teeforce-${{ inputs.environment }}" \
            --location ${{ env.LOCATION }} \
            --parameters infra/bicep/parameters.${{ inputs.environment }}.bicepparam \
            --parameters imageTag=${{ steps.current-image.outputs.tag }}
