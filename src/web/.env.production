# Production environment variables
# Loaded when running `pnpm build`

# Hide dev tools in production builds
# VITE_SHOW_DEV_TOOLS=false  # Commented out - undefined is falsy, so dev tools are hidden
